# AI Sales Intelligence Platform — one-command stack
# Linux / Windows / macOS:
#   docker compose up --build
#
# Services:
#   frontend  http://localhost:3000
#   backend   http://localhost:8000  (Swagger: /docs)
#   postgres  localhost:5432
#   pgadmin   http://localhost:5050
#   ollama    optional — docker compose --profile ai up --build

services:
  postgres:
    image: postgres:16-alpine
    container_name: aisales-postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-aisales}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-aisales_secret}
      POSTGRES_DB: ${POSTGRES_DB:-aisales}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-aisales} -d ${POSTGRES_DB:-aisales}"]
      interval: 5s
      timeout: 5s
      retries: 20

  pgadmin:
    image: dpage/pgadmin4:8
    container_name: aisales-pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@example.com
      PGADMIN_DEFAULT_PASSWORD: admin
      PGADMIN_CONFIG_SERVER_MODE: "False"
    ports:
      - "5050:80"
    depends_on:
      postgres:
        condition: service_healthy

  ollama:
    image: ollama/ollama:latest
    container_name: aisales-ollama
    profiles: ["ai"]
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama

  backend:
    build:
      context: .
      dockerfile: docker/backend/Dockerfile
    container_name: aisales-backend
    environment:
      DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER:-aisales}:${POSTGRES_PASSWORD:-aisales_secret}@postgres:5432/${POSTGRES_DB:-aisales}
      OLLAMA_BASE_URL: ${OLLAMA_BASE_URL:-http://ollama:11434}
      LLM_PROVIDER: ${LLM_PROVIDER:-ollama}
      CORS_ORIGINS: http://localhost:3000,http://127.0.0.1:3000,http://localhost:5173,http://127.0.0.1:5173,http://frontend
      SECRET_KEY: ${SECRET_KEY:-windows-dev-secret-key-change-me-32chars-min}
      BOOTSTRAP_ADMIN_EMAIL: ${BOOTSTRAP_ADMIN_EMAIL:-admin@example.com}
      BOOTSTRAP_ADMIN_USERNAME: ${BOOTSTRAP_ADMIN_USERNAME:-admin}
      BOOTSTRAP_ADMIN_PASSWORD: ${BOOTSTRAP_ADMIN_PASSWORD:-Admin123!}
      BOOTSTRAP_ADMIN_API_KEY: ${BOOTSTRAP_ADMIN_API_KEY:-dev-api-key-change-me}
      DEBUG: ${DEBUG:-true}
      APP_ENV: ${APP_ENV:-development}
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      # Require DB + admin seed (not just process up)
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/v1/ready"]
      interval: 5s
      timeout: 5s
      retries: 30
      start_period: 15s

  frontend:
    build:
      context: .
      dockerfile: docker/frontend/Dockerfile
      args:
        VITE_API_BASE_URL: /api/v1
    container_name: aisales-frontend
    ports:
      - "3000:80"
    depends_on:
      backend:
        condition: service_healthy

volumes:
  postgres_data:
  ollama_data:
